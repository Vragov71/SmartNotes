# 📝 NotesWebApp - Курсов проект

[cite_start]Това хранилище съдържа изходния код и документацията за уеб приложението **NotesWebApp**, разработено като курсов проект по модул "Интернет програмиране" към Национална програма "Обучение за ИТ умения и кариера"[cite: 1, 3].

## 📋 Описание на проекта

**NotesWebApp** е система за организиране на лична информация, която позволява на потребителите да създават, редактират и съхраняват бележки с възможност за прикачване на файлове и изображения.

[cite_start]Приложението е разработено съгласно изискванията за трислойна архитектура (Data, Service, Presentation layers) и използва актуални технологии от стека на Microsoft[cite: 5, 9].

## 🚀 Използвани технологии

* **Framework:** ASP.NET Core 9.0 (MVC)
* **Language:** C#
* **Database:** Microsoft SQL Server
* **ORM:** Entity Framework Core
* **Authentication:** ASP.NET Core Identity
* **Frontend:** Razor Views, HTML5, CSS3, Bootstrap
* **Tools:** Visual Studio / Rider, Git

## 🏗️ Архитектура и База от данни

[cite_start]Приложението спазва изискването за работа с поне 4 вида данни (entities) и наличие на връзка "много-към-много"[cite: 8].

### Същности (Entities):
1.  **Note:** Основната единица (Заглавие, Съдържание, Дата).
2.  **Tag:** Етикети за категоризация.
3.  **NoteImage:** Изображения, прикачени към бележката.
4.  **NoteFile:** Файлове, прикачени към бележката.

### Връзки (Relationships):
* **Many-to-Many:** Между `Note` и `Tag` (реализирана чрез `NoteTags`).
* **One-to-Many:** Между `Note` и `NoteImage`.
* **One-to-Many:** Между `Note` и `NoteFile`.
* **User Relation:** Всяка бележка е свързана с конкретен `UserId`.

## ✨ Функционалности

1.  **Потребителски профили:**
    * Регистрация и вход (Authentication).
    * Защита на данните (всеки вижда само своите бележки).
2.  **Управление на бележки (CRUD):**
    * Създаване на нови бележки.
    * Преглед на списък и детайли.
    * Редактиране и изтриване.
3.  **Файлови операции:**
    * Качване на изображения към бележка.
    * Качване на произволни файлове.
    * Изтриване на прикачени ресурси.
4.  **Категоризация:**
    * Добавяне на динамични етикети (tags) към всяка бележка.

## ⚙️ Инсталация и стартиране

За да стартирате проекта локално, следвайте тези стъпки:

1.  **Клониране на хранилището:**
    ```bash
    git clone [https://github.com/your-username/NotesWebApp.git](https://github.com/your-username/NotesWebApp.git)
    ```

2.  **Настройка на базата данни:**
    Отворете `appsettings.json` и се уверете, че `DefaultConnection` сочи към вашия SQL Server. След това изпълнете миграциите:
    ```bash
    dotnet ef database update
    ```

3.  **Стартиране на приложението:**
    ```bash
    dotnet run
    ```
    Приложението ще бъде достъпно на `https://localhost:7239` (или портът, конфигуриран във вашия `launchSettings.json`).

## 🧪 Тестване

Проектът включва набор от тестове в папката `Tests`. За изпълнение:
```bash
dotnet test